Read `AGENTS.md` first.

Then read:

* `<TASK_FILE>`
* the thesis and source files listed in the task
* `docs/ai/REPOSITORY_MAP_IFC_EDITOR.md` only if implementation details are relevant

Create the German-language thesis chapter described in `<TASK_FILE>` and write it directly to the specified LaTeX file.

Requirements:

* use formal, precise German academic language;
* follow the existing thesis structure, terminology and LaTeX conventions;
* use only the supplied sources and verified implementation evidence;
* do not invent sources, citations, results or implementation details;
* distinguish implemented functionality, design decisions, proposals, limitations and future work;
* preserve existing labels, citation keys and cross-references;
* modify only files required by the task;
* do not scan the complete repository;
* do not commit, push or merge.

Do not execute any Python, LaTeX, Git or review scripts. I will run all checks manually.

At the end, report briefly:

1. changed files;
2. chapter content created;
3. sources and implementation files used;
4. unresolved statements or missing evidence;
5. the next manual command I should run.
